<!DOCTYPE html>
<html>
<head>
  <title>Testdeviseapp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
<div class="container">
<div class="navbar navbar-inner">
    <a class="btn btn-navbar" data-toggle="collapse" data-target=".navbar-responsive-llapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </a>
      <a class="brand" href="#">Simple Posts App</a>
                  <div class="nav-collapse collapse navbar-responsive-collapse ">
                    <ul class="nav">
                      <li class="active"><% if user_signed_in? %><%= link_to "Home", controller: "posts" %> <% end -%></li>
                      <li class="dropdown">
                        <a href="/user_permission" class="dropdown-toggle" data-toggle="dropdown">Manage Roles <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                          <li><% if user_signed_in? %><%= link_to "Assign Permission", controller: "permission" %> <% end -%></li>
                          <li><% if user_signed_in? %><%= link_to "Create New Role", controller: "roles" %> <% end -%></li>
                          <li class="divider"></li>
                          <li class="nav-header">Nav header</li>
                          <li><a href="#">link - 1</a></li>
                          <li><a href="#">link - 2</a></li>
                        </ul>
                      </li>
                    </ul>
                    <form class="navbar-search pull-left" action="">
                      <input type="text" class="search-query span2" placeholder="Search">
                    </form>
                  </div><!-- /.nav-collapse -->
</div>
</div>
<div class="container">
 <!--link for login and Logout-->
  <div id="user_nav">
    <% if user_signed_in? %>
      Logged in as <strong><%= current_user.email %></strong>.
      <%= link_to 'Edit profile', edit_user_registration_path %> |
      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path %> |
      <%= link_to "Login", new_user_session_path %>
    <% end %>
  </div>

<div class="row">
	<div class="container "><%= yield %></div>

</div>
</div>
</body>
</html>
