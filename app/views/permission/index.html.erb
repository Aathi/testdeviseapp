
<%- model_class = User -%>
<%- model_class = Role -%>

<h2 id="user_details">User Details</h2>
<table border="0" cellspacing="5" cellpadding="5">
	<th>User ID</th>
	<th>Email Id</th>
	<th>Last Login</th>
	<th>Created Date</th>
	<th>Updated Date</th>
	<th>Permission</th>
	
	<%#= model_class.human_attribute_name(:id)%>
	<%#= model_class.human_attribute_name(:email)%>
	<%#= model_class.human_attribute_name(:last_sign_in_at)%>
	<%#= model_class.human_attribute_name(:created_at)%>
	<%#= model_class.human_attribute_name(:updated_at)%>
	<%#=t '.actions', :default => t("helpers.actions") %>
	
	<% @users.each do |user| %>
	<tr>
      	<td><%= user.id %></td>
      	<td><%= link_to user.email %></td>
		<td><%= user.last_sign_in_at %></td>
		<td><%= user.created_at %></td>
		<td><%= user.updated_at %></td>	
		<td><select name="roles[]">
		    <% Role.all.each do |role| %>
		        <option value="<%= role.role_name %>"><%= role.role_name %></option>
		    <% end %>
		</select>
		<td><%= link_to t('.new', :default => t("helpers.links.Update")),
            new_role_path,:class => 'btn btn-primary' %></td>
		</td>
		<!--<div class="form-actions">
  			<button type="submit" class="btn btn-primary">Save changes</button>
  			<button type="button" class="btn">Cancel</button>
		</div>-->
				
	<tr>
	<% end %>
</table>




